apiVersion: apps/v1
kind: Deployment
metadata:
  name: compass-frontend
  labels:
    app: compass-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: compass-frontend
  template:
    metadata:
      labels:
        app: compass-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/OWNER/compass-frontend:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: compass-frontend
spec:
  selector:
    app: compass-frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
